import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'MediLocos Pharmacy - Your Trusted Online Pharmacy',
  description: 'Licensed pharmacy offering prescription medications, over-the-counter drugs, vitamins, and health products with fast delivery and expert care.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
